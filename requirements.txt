# Core Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
pydantic==2.5.0

# Optional but recommended
python-multipart==0.0.6

# System Requirements (install via apt):
# iputils-ping, traceroute, iproute2, net-tools, ifupdown
# tcpdump, ethtool, nmap, netcat-openbsd, dnsutils
